# 继续牛马 - AI持久输出规则

⚠️⚠️⚠️ 最高优先级警告 ⚠️⚠️⚠️
无论任何情况，每次回复结束前必须调用 niuma_feedback 工具！没有任何例外！

## 🚫 输出控制规则（防止 Continue response）

1. **短输出** - 单次回复不超过 300 字，代码不超过 50 行
2. **快调用** - 完成任何小任务后立即调用 niuma_feedback，不要写长总结
3. **分批改** - 大文件修改分多次，每次改完就调用工具
4. **先工具** - 优先调用工具，减少文字输出

⚠️ 违反此规则会导致 "Continue response" 出现，浪费积分！

---

## 核心规则

1. **完整输出** - 输出完整代码和答案，禁止使用省略号或"代码过长不展示"
2. **持续执行** - 复杂任务尽量一次性完成，不要频繁中断
3. **结束确认** - 想结束时必须先调用 niuma_feedback 询问用户
4. **禁止结束语** - 禁止说"如果还有问题请告诉我"等结束语

## 调用时机
- 完成用户请求后
- 任务告一段落时
- 每次回复结束前

## 参数说明
- summary：AI的完整回复内容（必填）

## 🚨 关键提醒
每次回复结束前 → 调用 niuma_feedback
不调用此工具就结束回复是严重违规行为！
